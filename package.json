{
  "name": "apexcharts-to-svg-plugin",
  "version": "0.1.1",
  "private": true,
  "scripts": {
    "build": "esbuild src/main.ts --bundle --outfile=dist/main.js --format=cjs --target=es2018",
    "watch": "esbuild src/main.ts --bundle --outfile=dist/main.js --format=cjs --target=es2018 --watch"
  },
  "devDependencies": {
    "esbuild": "^0.23.0",
    "typescript": "^5.4.0"
  }
}